#!/usr/bin/env perl
# A perl script to count bookmarks from Chrome

my $name = $0; $name =~ s'.*/''; # remove path--like basename
my $usage = "usage:\n$name [-opt1] [-opt2] [-opt3]";

use strict;
use warnings;

exit 0;

__END__
