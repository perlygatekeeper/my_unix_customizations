	open(${1:IN},"<$${2:fromfile}") || die("$name: Cannot read from '$2': $!\n");
	open(${3:TO},">>${4:tofile}") || die("$name: Cannot append to '$4': $!\n");
	while (<$1>) {
		print $3 $_;
	}
	close($1);
	close($3);
