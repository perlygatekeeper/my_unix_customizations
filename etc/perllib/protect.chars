#!/usr/local/bin/perl
#
# A perl script to 'unwrap' *.tar files from the net.
#
$name = ' ^ $ ? * + ( ) { } [ ] < > \ / . a 1 " ';
$search=$name;
$search=~s/([\$\^*\[\]{}.<>()+?\/\\])/\\\1/g;
print "'$name'\n";
print "'$search'\n";
