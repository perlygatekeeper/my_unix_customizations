#!/usr/bin/perl
#
# A perl script to describe/resolve dependencies in php
# (and maybe later perl) code.
#
$name = $0; $name =~ s'.*/''; # remove path--like basename
$usage = "usage:\n$name [-tree]";

$last_mod_date='Wed, Mar 20, 2002';
$author="Dr. Steven Parker";

sub expand {
  my($filename,$depth)=@_;
}

exit 0;

__END__
